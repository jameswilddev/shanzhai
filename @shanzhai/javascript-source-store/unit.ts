import { javascriptSourceStore } from ".";

describe(`javascriptSourceStore`, () => {
  it(`uses the correct name`, () => {
    expect(javascriptSourceStore.name).toEqual(`javascriptSourceStore`);
  });
});
