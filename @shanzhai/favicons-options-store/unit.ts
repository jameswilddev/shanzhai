import { faviconsOptionsStore } from ".";

describe(`faviconsOptionsStore`, () => {
  it(`uses the correct name`, () => {
    expect(faviconsOptionsStore.name).toEqual(`faviconsOptionsStore`);
  });
});
