import { faviconsOptionsSourceStore } from ".";

describe(`faviconsOptionsSourceStore`, () => {
  it(`uses the correct name`, () => {
    expect(faviconsOptionsSourceStore.name).toEqual(
      `faviconsOptionsSourceStore`
    );
  });
});
